/*
 * e-Weed — Mega 2560 (Pro Mini / estándar ATmega2560)
 *
 * La implementación vive en los .cpp/.h copiados desde `controller_uno/src/`
 * (fuente canónica del monorepo). Este .ino solo documenta el sketch.
 *
 * Arduino IDE: instalar librerías DHT, OneWire, DallasTemperature, RTClib (Adafruit).
 * PlatformIO (recomendado): `cd controller_uno && pio run -e mega2560`
 */
